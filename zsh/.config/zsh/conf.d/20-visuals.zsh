# ==============================================================================
# SECTION 2: VISUALS & THEME ENGINE SETTINGS
# ==============================================================================
# Simplified knobs controlling banner and prompt engine selection.
# ------------------------------------------------------------------------------

# BANNER_MODE selects startup greeting style
typeset -g BANNER_MODE=classic
# PROMPT_PROFILE chooses custom prompt variant
typeset -g PROMPT_PROFILE=2
# PROMPT_ENGINE toggles between custom, starship, or p10k
typeset -g PROMPT_ENGINE=custom
