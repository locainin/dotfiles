########################
### waybar autostart ###
########################

# launch Waybar via helper script
# - uses ~/.config/hypr/waybar/config.jsonc + style.css + colors.css
# - falls back to XDG ~/.config/waybar if Hypr-specific modules are missing
exec-once = ~/.config/hypr/scripts/start-waybar.sh
